<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:label="SUPER3"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="true"
    android:supportsRtl="true"
    android:theme="@style/Theme.Super3">

    <activity
      android:name=".MainActivity"
      android:screenOrientation="fullSensor"
      android:exported="true">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="content" android:mimeType="application/zip" />
        <data android:scheme="content" android:mimeType="application/x-zip-compressed" />
        <data android:scheme="file" android:mimeType="application/zip" android:pathPattern=".*\\.zip" />
        <data android:scheme="file" android:mimeType="application/x-zip-compressed" android:pathPattern=".*\\.zip" />
        <data android:scheme="file" android:mimeType="application/octet-stream" android:pathPattern=".*\\.zip" />
      </intent-filter>

    </activity>

    <activity
      android:name=".SetupWizardActivity"
      android:screenOrientation="fullSensor"
      android:exported="false" />

    <activity
      android:name=".IniEditorActivity"
      android:screenOrientation="fullSensor"
      android:exported="false" />

    <activity
      android:name=".IniSettingsActivity"
      android:screenOrientation="fullSensor"
      android:exported="false" />

    <activity
      android:name=".Super3Activity"
      android:screenOrientation="sensorLandscape"
      android:exported="false">

      <!-- Tell SDL which native library to load -->
      <meta-data
        android:name="SDL_MAIN_LIBRARY"
        android:value="super3" />
    </activity>

  </application>
</manifest>
